      <div class="flexrow">
        <div class="resource flexrow flex-group-center">
          <span class="flex-item">
            <input type="text" name="system.hitDie" value="{{system.hitDie}}" placeholder="e.g., 1d8" /><br />
            <label for="system.hitDie">{{localize 'HYP3E.details.hitDie'}}</label>
          </span>
        </div>
        <div class="resource flexrow flex-group-center">
          <span class="flex-item">
            <select name="system.baseClass">
              <option value=""></option>
              {{selectOptions baseClasses selected=system.baseClass localize=false}}
            </select><br />
            <label for="system.baseClass" title="{{localize 'HYP3E.classEditor.baseClassNote'}}">{{localize 'HYP3E.classEditor.baseClass'}}</label>
            {{!-- <p style="font-style: italic;">{{localize 'HYP3E.classEditor.baseClassNote'}}</p> --}}
          </span>
        </div>
        <div class="resource flexrow flex-group-center">
          <div class="spellcaster-switch">
            <label class="switch">
              <input type="checkbox" class="spellcaster-toggle" 
                data-action="toggleSpells" 
                data-value="{{system.spellcaster}}" 
                {{#if system.spellcaster}}checked{{/if}}
              >
              <div class="slider round"></div>
            </label>
            <label class="header-label-small">
              {{#if system.spellcaster}}{{localize "HYP3E.dataLabel.spellcaster"}}
              {{else}}{{localize "HYP3E.dataLabel.noSpells"}}
              {{/if}}
            </label>
          </div>
        </div>
      </div>
      <div class="flexrow">
        <fieldset>
          <legend>{{localize 'HYP3E.classEditor.spellLists'}}</legend>
          {{!-- <p>{{localize 'HYP3E.classEditor.spellListsNote'}}</p> --}}
          <div class="flex flexrow flex-group-center form-group container">
            {{#each system.spellLists}}
            <span class="flex-item">
              <select name="system.spellLists.{{@index}}">
                <option value=""></option>
                {{selectOptions @root.spellLists selected=this localize=false}}
              </select>
              <br />
              <label for="system.spellLists.{{@index}}">{{localize 'HYP3E.classEditor.spellList'}} {{add @index 1}}</label>        
            </span>
            {{/each}}
          </div>
        </fieldset>
      </div>